import { styled } from '@styles/stitches.config'

export namespace CommonPageStyles {
  export const Page = styled('div', {
    display: 'flex',
    flexDirection: 'column',
    gap: '$medium',
  })
}
